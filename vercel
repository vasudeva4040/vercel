/** @type {import('next').NextConfig} */
const nextConfig = {
  async redirects() {
    return [
      // exact one link
      {
        source: "/xgrpcamx",
        destination: "https://www.whereby.com/xgrpcamx",
        permanent: false, // 307/308 style (safe)
      },

      // OPTIONAL: make any /<room> redirect to whereby.com/<room>
      // { source: "/:room", destination: "https://www.whereby.com/:room", permanent: false },
    ];
  },
};

module.exports = nextConfig;

